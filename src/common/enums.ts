export enum UserStatus {
  ACTIVE = 'active',
  INACTIVE = 'inactive',
}

export enum WalletStatus {
  ACTIVE = 'active',
  FROZEN = 'frozen',
}

export enum TransactionType {
  CREDIT = 'credit',
  DEBIT = 'debit',
}
